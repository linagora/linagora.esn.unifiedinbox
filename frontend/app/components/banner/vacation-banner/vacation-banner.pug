.inbox-banner(ng-if="$ctrl.vacationActivated")
  .flex-start.flex-grow
    span #{__('Your vacation responder is enabled.')}
  .flex-end
    button.btn.btn-link.inbox-banner-button(ng-click="$ctrl.disableVacation()") #{__('Disable')}
