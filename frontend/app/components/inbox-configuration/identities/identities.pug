sub-header
  inbox-identities-subheader

.inbox-identities
  inbox-identity(
    ng-repeat="identity in $ctrl.identities track by identity.id",
    identity-id="{{ identity.id }}",
    can-edit="$ctrl.canEdit"
  )
